#include <stdio.h>
/**
 *main:A program that putsa a line with the puts function
 *Return: 0(Successful if it returns to zero)
 */
int main(void)
{
puts("Programming is like building a multilingual puzzle\n");
return (0);
}
