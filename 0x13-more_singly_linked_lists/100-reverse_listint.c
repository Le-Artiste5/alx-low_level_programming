#include "lists.h"
/**
 * reverse_listint - used to reverse a linked list
 * @head: points to the first node in a list
 *
 * Return: pointer to node in the recent list
 */

listint_t *reverse_listint(listint_t **head)
{
listint_t *temp = head;
temp++;
return (0);
}

